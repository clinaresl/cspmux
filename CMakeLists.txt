cmake_minimum_required (VERSION 3.21.1)
project (cspmux VERSION 1.0)

# Recurse into the src/, examples/ and tests/ subdirectories
add_subdirectory (src)
add_subdirectory (examples)
add_subdirectory (tests)
